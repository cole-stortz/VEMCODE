{
  "name": "VEMCODE Dev + GUI",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/desktop-lite:1": {}
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y qt6-base-dev cmake g++ && cmake -B build -S . && cmake --build build -j$(nproc) && nohup ./app/VEMCODE > /tmp/vemcode.log 2>&1 &",
  "forwardPorts": [6080],
  "portsAttributes": {
    "6080": {
      "label": "VEMCODE",
      "onAutoForward": "openBrowserOnce"
    }
  }
}
